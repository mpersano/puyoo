v fix dropping
v state for marking 
v rotation animation
v sprite/texture abstraction
v get rid of fopen/fread/fclose (sucks on libpsx)
? clean chain detection (explicit stack?)
? hiccups when dropping blocks
* explosion animation
  * sparks before explosion
  v explosion sprites
v move falling block to right/left when rotating on left/right corners
v jama blocks
  v dropping jama state
  v kill jama when next to chain
  v send jama to opponent
  v max # of jama per column
  v clip jama sprites outside grid area when dropping
* opponent AI
v text render
* grid
  * grid background
  * grid foreground
  * game over state
  * score
    * increment score
    * score display
* "combo" animation
* game states
  * menu
  * in-game
